# Reusable Components

KPI cards, status badges, searchable tables, filter controls, employee identity blocks, detail panels, evidence rows, timelines, audit issue rows, empty states, command buttons, and responsive navigation are shared across screens.
