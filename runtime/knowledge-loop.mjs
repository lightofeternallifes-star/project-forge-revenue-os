export function createKnowledgeFeedback(mission, employee) {
  const domain = employee.knowledgeProfile?.domains?.[0] || mission.type;
  return { lessonsLearned: mission.context.lessonsLearned || `Execution of ${mission.title} produced a reusable ${mission.type.toLowerCase()} operating record for ${domain}.`, reusableKnowledge: mission.context.reusableKnowledge || `Mission pattern: define objective, load ${domain} context, execute within permission boundaries, attach evidence, and review outcome.`, referencedDocuments: employee.documents.slice(0, 3).map((document) => document.reference), relatedMissions: employee.missionHistory.map((item) => item.id), recommendedImprovements: mission.context.recommendedImprovements || 'Review execution evidence and refine the mission brief before the next assignment.', futureAutomationCandidates: mission.context.automationCandidates || 'Identify repeated evidence collection or reporting steps for factory automation.', createdAt: new Date().toISOString() };
}
