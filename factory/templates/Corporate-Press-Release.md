# Corporate Press Release Template

## `{{organization_name}}` Announces the Graduation of `{{employee_name}}`

`{{organization_name}}` announces the graduation of `{{employee_name}}` (`{{employee_number}}`), `{{role}}` in `{{division}}`, after completion of the approved Digital Employee lifecycle.

The employee completed `{{mission_id}}` and will serve within the approved scope of `{{responsibilities}}`.

Publication status: `{{publication_status}}`

This release may be published only after Communications, Legal, Executive Office, and brand approval.
