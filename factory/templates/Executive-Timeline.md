# Executive Timeline Template

## `{{employee_number}}` `{{employee_name}}`

| Date | Event | Evidence | Actor |
| --- | --- | --- | --- |
| `{{event_date}}` | `{{event_name}}` | `{{event_evidence}}` | `{{event_actor}}` |

Every material lifecycle transition must append a row. Dates must use ISO format and remain ordered.
